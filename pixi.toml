[project]
authors = ["Paul Link <paullink65@gmail.com>"]
channels = ["conda-forge"]
description = "Add a short description here"
name = "compiler"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
c = "git commit -m"

[dependencies]
pytest = ">=8.3.5,<9"
black = ">=25.1.0,<26"
pre-commit = ">=4.2.0,<5"

[pypi-dependencies]
compiler = { path = ".", editable = true }
